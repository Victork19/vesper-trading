[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: requires PostgreSQL and external service fixtures
    fault_injection: crash, timeout, and venue failure scenarios
    controlled_account: tests against the deterministic controlled venue
